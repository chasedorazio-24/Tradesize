<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="TradeSize">
<title>TradeSize</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@300;400;500;600;700;800&family=Barlow:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}

:root {
  --bg:        #080b12;
  --bg2:       #0d1220;
  --panel:     #111826;
  --panel2:    #16202f;
  --panel3:    #1c2a3d;
  --line:      rgba(255,255,255,0.07);
  --line2:     rgba(255,255,255,0.13);
  --blue:      #3b82f6;
  --blue-bright:#60a5fa;
  --blue-glow: rgba(59,130,246,0.18);
  --blue-soft: rgba(59,130,246,0.08);
  --blue-dim:  rgba(59,130,246,0.04);
  --cyan:      #22d3ee;
  --red:       #f43f5e;
  --red-soft:  rgba(244,63,94,0.1);
  --green:     #10b981;
  --green-soft:rgba(16,185,129,0.1);
  --text:      #f1f5f9;
  --text2:     rgba(241,245,249,0.55);
  --text3:     rgba(241,245,249,0.28);
  --text4:     rgba(241,245,249,0.12);
  --mono:      'DM Mono', monospace;
  --head:      'Barlow Condensed', sans-serif;
  --body:      'Barlow', sans-serif;
}

html,body{height:100%;background:var(--bg);color:var(--text);font-family:var(--body);overflow-x:hidden;-webkit-font-smoothing:antialiased}

/* Subtle scan-line texture */
body::before {
  content:'';position:fixed;inset:0;
  background: repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(0,0,0,0.03) 2px, rgba(0,0,0,0.03) 4px);
  pointer-events:none;z-index:0;
}

/* Deep ambient gradient */
body::after {
  content:'';position:fixed;
  top:-40%;left:-20%;width:80%;height:80%;
  background:radial-gradient(ellipse at 30% 40%, rgba(59,130,246,0.06) 0%, transparent 60%);
  pointer-events:none;z-index:0;
}

.app{position:relative;z-index:1;max-width:430px;margin:0 auto;min-height:100svh;display:flex;flex-direction:column;padding-bottom:48px}

/* ══ STATUS BAR ══ */
.status-bar {
  display:flex;align-items:center;justify-content:space-between;
  padding:52px 20px 0;
  margin-bottom:20px;
}

.brand {
  display:flex;align-items:baseline;gap:2px;
}

.brand-mark {
  font-family:var(--head);
  font-size:11px;font-weight:700;
  letter-spacing:3px;text-transform:uppercase;
  color:var(--text3);
}

.ticker-live {
  display:flex;align-items:center;gap:8px;
}

.dot-pulse {
  width:6px;height:6px;border-radius:50%;
  background:var(--green);
  box-shadow:0 0 0 0 rgba(16,185,129,0.4);
  animation:ping 2s ease-in-out infinite;
}
.dot-pulse.off{background:var(--text3);box-shadow:none;animation:none}

@keyframes ping {
  0%,100%{box-shadow:0 0 0 0 rgba(16,185,129,0.5)}
  50%{box-shadow:0 0 0 5px rgba(16,185,129,0)}
}

.live-text {
  font-family:var(--head);
  font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  color:var(--text3);
}
.live-text.on{color:var(--green)}

/* ══ HERO SECTION ══ */
.hero {
  padding:0 20px 28px;
  border-bottom:1px solid var(--line);
}

.hero-title {
  font-family:var(--head);
  font-size:48px;font-weight:800;
  letter-spacing:-1px;line-height:0.95;
  color:var(--text);
  margin-bottom:6px;
}

.hero-title em {
  font-style:normal;
  color:var(--blue-bright);
  position:relative;
}

.hero-sub {
  font-family:var(--head);
  font-size:12px;font-weight:500;letter-spacing:2px;text-transform:uppercase;
  color:var(--text3);
}

/* ══ METRIC STRIP ══ */
.metric-strip {
  display:grid;grid-template-columns:1fr 1px 1fr 1px 1fr;
  margin:0 20px;
  padding:0;
  background:var(--panel);
  border:1px solid var(--line2);
  border-radius:14px;
  overflow:hidden;
  margin-bottom:16px;
}

.metric-divider{background:var(--line)}

.metric {
  padding:14px 16px;
  position:relative;
}

.metric-label {
  font-family:var(--head);
  font-size:9px;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  color:var(--text3);margin-bottom:5px;
}

.metric-val {
  font-family:var(--head);
  font-size:18px;font-weight:700;
  color:var(--text2);
  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
  transition:color 0.3s;
}
.metric-val.active{color:var(--text)}
.metric-val.blue{color:var(--blue-bright)}
.metric-val.red{color:var(--red)}

/* ══ API ROW ══ */
.api-row {
  margin:0 20px 10px;
  background:var(--panel);
  border:1px solid var(--line);
  border-radius:12px;
  overflow:hidden;
}

.api-header {
  display:flex;align-items:center;gap:12px;
  padding:13px 16px;
  cursor:pointer;
  transition:background 0.15s;
  user-select:none;
}
.api-header:hover{background:rgba(255,255,255,0.02)}

.api-pill {
  font-family:var(--head);
  font-size:9px;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  padding:4px 9px;border-radius:4px;
  background:var(--blue-soft);color:var(--blue-bright);
  border:1px solid rgba(59,130,246,0.2);
  flex-shrink:0;
}
.api-pill.off{background:rgba(255,255,255,0.04);color:var(--text3);border-color:var(--line)}

.api-label{font-family:var(--head);font-size:13px;font-weight:600;color:var(--text2);flex:1}
.api-chevron{font-size:16px;color:var(--text3);transition:transform 0.25s}
.api-chevron.open{transform:rotate(90deg)}

.api-body {
  max-height:0;overflow:hidden;transition:max-height 0.3s ease;
  border-top:0px solid var(--line);
}
.api-body.open{max-height:80px;border-top-width:1px}

.api-inner {
  display:flex;gap:8px;padding:12px 14px;
}

.api-inner input {
  flex:1;background:var(--panel2);
  border:1px solid var(--line2);border-radius:8px;
  padding:9px 12px;
  font-family:var(--mono);font-size:12px;color:var(--text);
  outline:none;transition:border-color 0.2s;
}
.api-inner input:focus{border-color:rgba(59,130,246,0.5)}
.api-inner input::placeholder{color:var(--text4)}

.api-btn {
  background:var(--blue);color:#fff;
  border:none;border-radius:8px;
  padding:9px 16px;
  font-family:var(--head);font-size:13px;font-weight:700;
  cursor:pointer;transition:background 0.2s;white-space:nowrap;
}
.api-btn:hover{background:var(--blue-bright)}

/* ══ FORM CARD ══ */
.form-card {
  margin:0 20px;
  background:var(--panel);
  border:1px solid var(--line);
  border-radius:20px;
  overflow:hidden;
}

/* Ticker section */
.ticker-section {
  padding:20px 20px 0;
}

.field-label {
  font-family:var(--head);
  font-size:9.5px;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  color:var(--text3);margin-bottom:8px;padding-left:1px;
  display:flex;align-items:center;gap:8px;
}

.auto-tag {
  font-size:8px;letter-spacing:1.5px;
  padding:2px 7px;border-radius:3px;
  background:var(--blue-soft);color:var(--blue);
  border:1px solid rgba(59,130,246,0.2);
}

.ticker-wrap {
  position:relative;
}

#tickerInput {
  width:100%;
  background:var(--panel2);
  border:1px solid var(--line2);
  border-radius:12px;
  padding:16px 20px;
  font-family:var(--head);
  font-size:32px;font-weight:800;letter-spacing:2px;
  text-transform:uppercase;
  color:var(--text);
  outline:none;
  transition:border-color 0.2s,box-shadow 0.2s,background 0.2s;
  caret-color:var(--blue);
  -webkit-appearance:none;
}
#tickerInput:focus{
  border-color:rgba(59,130,246,0.5);
  background:var(--panel3);
  box-shadow:0 0 0 3px rgba(59,130,246,0.08), inset 0 1px 0 rgba(255,255,255,0.04);
}
#tickerInput::placeholder{font-size:24px;font-weight:500;letter-spacing:0;color:var(--text4)}

/* Market data row */
.mkt-row {
  display:grid;grid-template-columns:1fr 1fr;
  gap:8px;margin-top:10px;
}

.mkt-box {
  background:var(--panel2);
  border:1px solid var(--line);
  border-radius:10px;
  padding:12px 14px;
  min-height:62px;
  display:flex;flex-direction:column;justify-content:space-between;
  position:relative;overflow:hidden;
  transition:border-color 0.3s;
}
.mkt-box.loaded{border-color:rgba(59,130,246,0.25)}
.mkt-box.loaded.stop-box{border-color:rgba(244,63,94,0.2)}

.mkt-box-label {
  font-family:var(--head);
  font-size:8.5px;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  color:var(--text3);
}

.mkt-box-val {
  font-family:var(--head);
  font-size:22px;font-weight:700;
  color:var(--text3);
  transition:color 0.3s;
  line-height:1;
}
.mkt-box-val.loaded{color:var(--blue-bright)}
.mkt-box-val.stop-loaded{color:var(--red)}

/* Shimmer */
.mkt-box.fetching::after{
  content:'';position:absolute;inset:0;
  background:linear-gradient(90deg,transparent,rgba(59,130,246,0.06),transparent);
  animation:sweep 1.2s infinite;
}
@keyframes sweep{0%{transform:translateX(-100%)}100%{transform:translateX(100%)}}

/* ══ DIVIDER WITH LABEL ══ */
.section-sep {
  display:flex;align-items:center;gap:12px;
  padding:18px 20px 0;
}
.sep-line{flex:1;height:1px;background:var(--line)}
.sep-label {
  font-family:var(--head);
  font-size:9px;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  color:var(--text4);
}

/* ══ INPUTS GRID ══ */
.inputs-grid {
  display:grid;grid-template-columns:1fr 1fr;
  gap:8px;
  padding:12px 20px 0;
}

.input-group{display:flex;flex-direction:column;gap:7px}

.inp-wrap{position:relative;display:flex;align-items:center}

.inp-pre, .inp-suf {
  position:absolute;
  font-family:var(--head);font-size:15px;font-weight:600;
  color:var(--text3);pointer-events:none;z-index:1;
}
.inp-pre{left:14px}
.inp-suf{right:14px}

input[type=number],input[type=text] {
  width:100%;
  background:var(--panel2);
  border:1px solid var(--line2);
  border-radius:12px;
  padding:15px 16px;
  font-family:var(--head);font-size:20px;font-weight:700;
  color:var(--text);
  outline:none;
  -webkit-appearance:none;appearance:none;
  transition:border-color 0.2s,box-shadow 0.2s,background 0.2s;
  caret-color:var(--blue);
}
input[type=number]:focus,input[type=text]:focus{
  border-color:rgba(59,130,246,0.5);
  background:var(--panel3);
  box-shadow:0 0 0 3px rgba(59,130,246,0.07);
}
input::placeholder{color:var(--text4);font-weight:500}
input.has-pre{padding-left:28px}
input.has-suf{padding-right:36px}
input::-webkit-inner-spin-button,input::-webkit-outer-spin-button{-webkit-appearance:none}

/* Stop loss full width */
.stop-wrap {
  padding:8px 20px 0;
}
.stop-inner {
  position:relative;
}
.stop-inner .inp-pre{font-size:17px;top:50%;transform:translateY(-50%)}

#stopInput {
  width:100%;
  background:var(--panel2);
  border:1px solid var(--line2);
  border-radius:12px;
  padding:15px 16px 15px 30px;
  font-family:var(--head);font-size:22px;font-weight:700;
  color:var(--text);
  outline:none;-webkit-appearance:none;
  transition:border-color 0.2s,box-shadow 0.2s,background 0.2s;
  caret-color:var(--blue);
}
#stopInput:focus{
  border-color:rgba(244,63,94,0.5);
  background:var(--panel3);
  box-shadow:0 0 0 3px rgba(244,63,94,0.07);
}

.stop-auto-label {
  position:absolute;right:14px;top:50%;transform:translateY(-50%);
  font-family:var(--head);font-size:9px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;
  background:rgba(244,63,94,0.12);color:var(--red);
  border:1px solid rgba(244,63,94,0.25);
  border-radius:4px;padding:3px 8px;
  opacity:0;transition:opacity 0.3s;pointer-events:none;
}
.stop-auto-label.show{opacity:1}

/* ══ CTA BUTTON ══ */
.cta-wrap{padding:20px 20px 22px}

.cta {
  width:100%;
  background:linear-gradient(135deg,#2563eb 0%,#3b82f6 50%,#1d4ed8 100%);
  color:#fff;border:none;border-radius:14px;
  padding:19px 24px;
  font-family:var(--head);font-size:17px;font-weight:800;letter-spacing:1px;text-transform:uppercase;
  cursor:pointer;
  display:flex;align-items:center;justify-content:center;gap:12px;
  position:relative;overflow:hidden;
  transition:transform 0.15s,box-shadow 0.2s,opacity 0.2s;
  box-shadow:0 4px 24px rgba(37,99,235,0.3),inset 0 1px 0 rgba(255,255,255,0.15);
}
.cta::before{
  content:'';position:absolute;inset:0;
  background:linear-gradient(135deg,rgba(255,255,255,0.12) 0%,transparent 60%);
  pointer-events:none;
}
.cta:hover{transform:translateY(-2px);box-shadow:0 8px 32px rgba(37,99,235,0.45),inset 0 1px 0 rgba(255,255,255,0.15)}
.cta:active{transform:translateY(0);box-shadow:0 2px 12px rgba(37,99,235,0.2)}
.cta.busy{opacity:0.6;pointer-events:none}

.cta-icon{font-size:16px;transition:transform 0.2s}
.cta:hover .cta-icon{transform:translateX(4px)}

/* Error */
.err {
  margin:0 20px 10px;padding:11px 16px;
  background:var(--red-soft);border:1px solid rgba(244,63,94,0.25);
  border-radius:9px;
  font-family:var(--head);font-size:13px;font-weight:600;color:var(--red);
  display:none;
}
.err.on{display:block}

/* ══ RESULT CARD ══ */
.result {
  margin:12px 20px 0;
  background:var(--panel);
  border:1px solid var(--line);
  border-radius:20px;
  overflow:hidden;
  opacity:0;transform:translateY(20px);
  transition:opacity 0.4s cubic-bezier(.22,1,.36,1),transform 0.4s cubic-bezier(.22,1,.36,1);
  pointer-events:none;
}
.result.on{opacity:1;transform:translateY(0);pointer-events:auto}

/* Result top bar */
.result-top {
  background:linear-gradient(135deg,var(--panel2) 0%,var(--panel3) 100%);
  padding:20px 22px 18px;
  border-bottom:1px solid var(--line);
  display:flex;align-items:flex-start;justify-content:space-between;
}

.result-sym {
  font-family:var(--head);
  font-size:36px;font-weight:800;letter-spacing:1px;
  color:var(--text);line-height:1;
}

.result-meta {
  text-align:right;
}

.result-time {
  font-family:var(--mono);font-size:11px;color:var(--text3);
  margin-bottom:4px;
}

.result-regime {
  font-family:var(--head);font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;
  color:var(--blue);
}

/* Big number */
.result-hero {
  padding:28px 22px 24px;
  text-align:center;
  border-bottom:1px solid var(--line);
  background:var(--blue-dim);
  position:relative;
}

.result-hero::before{
  content:'';position:absolute;
  top:0;left:0;right:0;height:1px;
  background:linear-gradient(90deg,transparent,rgba(59,130,246,0.3),transparent);
}

.big-label {
  font-family:var(--head);
  font-size:10px;font-weight:700;letter-spacing:3px;text-transform:uppercase;
  color:var(--blue);margin-bottom:8px;
}

.big-num {
  font-family:var(--head);
  font-size:80px;font-weight:800;letter-spacing:-3px;line-height:1;
  color:var(--text);
  text-shadow:0 0 60px rgba(59,130,246,0.25);
}

.big-unit {
  font-family:var(--head);font-size:16px;font-weight:600;
  color:var(--text3);margin-top:4px;letter-spacing:1px;text-transform:uppercase;
}

/* Stats row */
.result-stats {
  display:grid;grid-template-columns:repeat(3,1fr);
  border-bottom:1px solid var(--line);
}

.rs {
  padding:16px 14px;
  border-right:1px solid var(--line);
  text-align:center;
}
.rs:last-child{border-right:none}

.rs-label {
  font-family:var(--head);font-size:9px;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  color:var(--text3);margin-bottom:5px;
}

.rs-val {
  font-family:var(--head);font-size:17px;font-weight:700;
  color:var(--text);
}
.rs-val.b{color:var(--blue-bright)}
.rs-val.r{color:var(--red)}
.rs-val.g{color:var(--green)}

/* Cost row */
.result-cost {
  display:flex;align-items:center;justify-content:space-between;
  padding:16px 22px;
}
.rc-label{font-family:var(--head);font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:var(--text3)}
.rc-val{font-family:var(--head);font-size:18px;font-weight:700;color:var(--text)}

/* ══ TOAST ══ */
.toast {
  position:fixed;bottom:28px;left:50%;
  transform:translateX(-50%) translateY(60px);
  background:var(--panel3);
  border:1px solid var(--line2);
  border-radius:100px;padding:11px 20px;
  font-family:var(--head);font-size:13px;font-weight:700;letter-spacing:0.5px;
  color:var(--text);white-space:nowrap;
  z-index:999;
  transition:transform 0.35s cubic-bezier(.34,1.56,.64,1);
  box-shadow:0 8px 32px rgba(0,0,0,0.5);
}
.toast.on{transform:translateX(-50%) translateY(0)}
.toast.ok{color:var(--green);border-color:rgba(16,185,129,0.3)}
.toast.fail{color:var(--red);border-color:rgba(244,63,94,0.3)}

.safe{height:max(env(safe-area-inset-bottom,16px),16px)}
</style>
</head>
<body>
<div class="app">

  <!-- STATUS BAR -->
  <div class="status-bar">
    <div class="brand"><span class="brand-mark">TradeSize · v2</span></div>
    <div class="ticker-live">
      <div class="dot-pulse off" id="liveDot"></div>
      <span class="live-text" id="liveText">OFFLINE</span>
    </div>
  </div>

  <!-- HERO -->
  <div class="hero">
    <div class="hero-title">SIZE YOUR<br><em>POSITION</em></div>
    <div class="hero-sub">Risk-based · Real-time · Day Low Auto-fill</div>
  </div>

  <!-- METRICS -->
  <div class="metric-strip">
    <div class="metric">
      <div class="metric-label">Account</div>
      <div class="metric-val blue" id="mAccount">—</div>
    </div>
    <div class="metric-divider"></div>
    <div class="metric">
      <div class="metric-label">Risk %</div>
      <div class="metric-val" id="mRisk">—</div>
    </div>
    <div class="metric-divider"></div>
    <div class="metric">
      <div class="metric-label">Max Loss</div>
      <div class="metric-val red" id="mLoss">—</div>
    </div>
  </div>

  <!-- API KEY -->
  <div class="api-row">
    <div class="api-header" id="apiToggle">
      <span class="api-pill off" id="apiPill">KEY</span>
      <span class="api-label">Finnhub API Key</span>
      <span style="font-family:var(--head);font-size:11px;font-weight:600;color:var(--text3)" id="apiStatusTxt">Not connected</span>
      <span class="api-chevron" id="apiChevron">›</span>
    </div>
    <div class="api-body" id="apiBody">
      <div class="api-inner">
        <input type="text" id="apiKeyInput" placeholder="Enter Finnhub API key…" autocomplete="off" spellcheck="false">
        <button class="api-btn" id="apiSave">Save</button>
      </div>
    </div>
  </div>

  <!-- FORM CARD -->
  <div class="form-card">

    <div class="ticker-section">
      <div class="field-label">Symbol</div>
      <div class="ticker-wrap">
        <input type="text" id="tickerInput" placeholder="NVDA" autocomplete="off" spellcheck="false" autocapitalize="characters">
      </div>

      <div class="mkt-row">
        <div class="mkt-box" id="entryBox">
          <div class="mkt-box-label">Entry Price</div>
          <div class="mkt-box-val" id="entryVal">—</div>
        </div>
        <div class="mkt-box stop-box" id="lowBox">
          <div class="mkt-box-label">Day's Low</div>
          <div class="mkt-box-val" id="lowVal">—</div>
        </div>
      </div>
    </div>

    <div class="section-sep">
      <div class="sep-line"></div>
      <div class="sep-label">Stop Loss</div>
      <div class="sep-line"></div>
    </div>

    <div class="stop-wrap">
      <div class="field-label">
        Stop Loss Price
        <span class="auto-tag" id="autoTag" style="display:none">AUTO-FILLED</span>
      </div>
      <div class="stop-inner">
        <span class="inp-pre" style="left:14px;color:var(--text3)">$</span>
        <input type="number" id="stopInput" placeholder="0.00" step="0.01" min="0">
        <span class="stop-auto-label" id="stopBadge">AUTO</span>
      </div>
    </div>

    <div class="section-sep">
      <div class="sep-line"></div>
      <div class="sep-label">Parameters</div>
      <div class="sep-line"></div>
    </div>

    <div class="inputs-grid">
      <div class="input-group">
        <div class="field-label">Account Size</div>
        <div class="inp-wrap">
          <span class="inp-pre">$</span>
          <input type="number" id="accountInput" class="has-pre" placeholder="10000" step="100" min="0">
        </div>
      </div>
      <div class="input-group">
        <div class="field-label">Risk %</div>
        <div class="inp-wrap">
          <input type="number" id="riskInput" class="has-suf" placeholder="1" step="0.1" min="0.01" max="100">
          <span class="inp-suf">%</span>
        </div>
      </div>
    </div>

    <div class="err" id="errBox"></div>
    <div class="cta-wrap">
      <button class="cta" id="calcBtn">
        <span>Calculate Position</span>
        <span class="cta-icon">→</span>
      </button>
    </div>

  </div>

  <!-- RESULT -->
  <div class="result" id="resultCard">
    <div class="result-top">
      <div class="result-sym" id="rSym">—</div>
      <div class="result-meta">
        <div class="result-time" id="rTime">—</div>
        <div class="result-regime">LONG ENTRY</div>
      </div>
    </div>
    <div class="result-hero">
      <div class="big-label">Shares to Buy</div>
      <div class="big-num" id="rShares">0</div>
      <div class="big-unit">shares</div>
    </div>
    <div class="result-stats">
      <div class="rs">
        <div class="rs-label">Entry</div>
        <div class="rs-val b" id="rEntry">—</div>
      </div>
      <div class="rs">
        <div class="rs-label">Stop</div>
        <div class="rs-val r" id="rStop">—</div>
      </div>
      <div class="rs">
        <div class="rs-label">Max Loss</div>
        <div class="rs-val r" id="rLoss">—</div>
      </div>
    </div>
    <div class="result-cost">
      <div class="rc-label">Total Cost</div>
      <div class="rc-val" id="rCost">—</div>
    </div>
  </div>

  <div class="safe"></div>
</div>

<div class="toast" id="toast"></div>

<script>
let apiKey = localStorage.getItem('ts_apikey') || '';
let livePrice = null, dayLow = null, fetchT = null;

// Init
window.addEventListener('DOMContentLoaded', () => {
  if (apiKey) { setApiConnected(true); document.getElementById('apiKeyInput').value = apiKey; }
  const acc = localStorage.getItem('ts_account');
  const risk = localStorage.getItem('ts_risk');
  if (acc) document.getElementById('accountInput').value = acc;
  if (risk) document.getElementById('riskInput').value = risk;
  updateMetrics();
});

// ── API KEY
document.getElementById('apiToggle').addEventListener('click', () => {
  const body = document.getElementById('apiBody');
  const chev = document.getElementById('apiChevron');
  const open = body.classList.toggle('open');
  chev.classList.toggle('open', open);
});

document.getElementById('apiSave').addEventListener('click', () => {
  const v = document.getElementById('apiKeyInput').value.trim();
  if (!v) { toast('Enter a key first', 'fail'); return; }
  apiKey = v;
  localStorage.setItem('ts_apikey', v);
  setApiConnected(true);
  document.getElementById('apiBody').classList.remove('open');
  document.getElementById('apiChevron').classList.remove('open');
  toast('API key saved', 'ok');
});

function setApiConnected(on) {
  const pill = document.getElementById('apiPill');
  const txt = document.getElementById('apiStatusTxt');
  const dot = document.getElementById('liveDot');
  const lt = document.getElementById('liveText');
  if (on) {
    pill.textContent = 'LIVE'; pill.className = 'api-pill';
    txt.textContent = 'Connected'; txt.style.color = 'var(--green)';
    dot.classList.remove('off'); lt.textContent = 'LIVE'; lt.className = 'live-text on';
  } else {
    pill.textContent = 'KEY'; pill.className = 'api-pill off';
    txt.textContent = 'Not connected'; txt.style.color = '';
    dot.classList.add('off'); lt.textContent = 'OFFLINE'; lt.className = 'live-text';
  }
}

// ── TICKER INPUT
document.getElementById('tickerInput').addEventListener('input', e => {
  const v = e.target.value.trim().toUpperCase();
  e.target.value = v;
  resetMarketData();
  clearTimeout(fetchT);
  if (v.length >= 1) fetchT = setTimeout(() => fetchQuote(v), 550);
});

function resetMarketData() {
  livePrice = null; dayLow = null;
  setBox('entryBox', 'entryVal', '—', false, false);
  setBox('lowBox', 'lowVal', '—', false, true);
  document.getElementById('stopInput').value = '';
  document.getElementById('stopBadge').classList.remove('show');
  document.getElementById('autoTag').style.display = 'none';
}

async function fetchQuote(sym) {
  if (!apiKey) { toast('Add Finnhub API key first', 'fail'); return; }
  setBox('entryBox', 'entryVal', '', false, false, true);
  setBox('lowBox', 'lowVal', '', false, true, true);
  try {
    const r = await fetch(`https://finnhub.io/api/v1/quote?symbol=${sym}&token=${apiKey}`);
    const d = await r.json();
    if (!d || !d.c || d.c === 0) {
      setBox('entryBox', 'entryVal', 'N/A', false, false);
      setBox('lowBox', 'lowVal', '—', false, true);
      toast(`"${sym}" not found`, 'fail'); return;
    }
    livePrice = d.c;
    dayLow = (d.l && d.l > 0) ? d.l : null;
    setBox('entryBox', 'entryVal', '$' + fmt(livePrice), true, false);
    if (dayLow) {
      setBox('lowBox', 'lowVal', '$' + fmt(dayLow), true, true);
      document.getElementById('stopInput').value = dayLow.toFixed(2);
      document.getElementById('stopBadge').classList.add('show');
      document.getElementById('autoTag').style.display = 'inline-flex';
    } else {
      setBox('lowBox', 'lowVal', 'Pre-mkt', false, true);
      toast('Day low unavailable — enter stop manually', 'fail');
    }
    updateMetrics();
    toast(`${sym} loaded`, 'ok');
  } catch {
    setBox('entryBox', 'entryVal', 'Error', false, false);
    toast('Fetch failed — check API key', 'fail');
  }
}

function setBox(boxId, valId, text, loaded, isStop, fetching = false) {
  const box = document.getElementById(boxId);
  const val = document.getElementById(valId);
  val.textContent = text;
  box.classList.toggle('fetching', fetching);
  box.classList.toggle('loaded', loaded);
  val.className = 'mkt-box-val' + (loaded ? (isStop ? ' stop-loaded' : ' loaded') : '');
}

// ── METRICS
['accountInput','riskInput'].forEach(id => {
  document.getElementById(id).addEventListener('input', () => {
    localStorage.setItem('ts_' + id.replace('Input',''), document.getElementById(id).value);
    updateMetrics();
  });
});

function updateMetrics() {
  const acc = parseFloat(document.getElementById('accountInput').value) || null;
  const risk = parseFloat(document.getElementById('riskInput').value) || null;
  document.getElementById('mAccount').textContent = acc ? '$' + fmtK(acc) : '—';
  document.getElementById('mRisk').textContent = risk ? risk + '%' : '—';
  if (acc && risk) {
    document.getElementById('mLoss').textContent = '$' + fmt(acc * risk / 100);
    document.getElementById('mAccount').classList.add('active');
  } else {
    document.getElementById('mLoss').textContent = '—';
  }
}

// ── CALCULATE
document.getElementById('calcBtn').addEventListener('click', calc);

function calc() {
  hideErr();
  const sym = document.getElementById('tickerInput').value.trim().toUpperCase();
  const stop = parseFloat(document.getElementById('stopInput').value);
  const acc = parseFloat(document.getElementById('accountInput').value);
  const risk = parseFloat(document.getElementById('riskInput').value);
  const entry = livePrice;

  if (!sym)         { showErr('Enter a ticker symbol'); return; }
  if (!entry)       { showErr('No live price — enter a symbol first'); return; }
  if (!stop || isNaN(stop)) { showErr('Stop loss price is required'); return; }
  if (stop >= entry){ showErr(`Stop ($${stop}) must be below entry ($${fmt(entry)})`); return; }
  if (!acc || acc <= 0)     { showErr('Enter your account size'); return; }
  if (!risk || risk <= 0 || risk > 100) { showErr('Enter a valid risk % (0.01–100)'); return; }

  const maxLoss = acc * (risk / 100);
  const dist = entry - stop;
  const shares = Math.floor(maxLoss / dist);
  const actualLoss = shares * dist;
  const cost = shares * entry;

  document.getElementById('rSym').textContent = sym;
  document.getElementById('rTime').textContent = new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'});
  document.getElementById('rShares').textContent = shares.toLocaleString();
  document.getElementById('rEntry').textContent = '$' + fmt(entry);
  document.getElementById('rStop').textContent = '$' + fmt(stop);
  document.getElementById('rLoss').textContent = '$' + fmt(actualLoss);
  document.getElementById('rCost').textContent = '$' + fmtK(cost);

  const card = document.getElementById('resultCard');
  card.classList.add('on');
  setTimeout(() => card.scrollIntoView({behavior:'smooth',block:'nearest'}), 80);
}

// ── UTILS
function fmt(n) {
  return parseFloat(n).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
}
function fmtK(n) {
  if (n >= 1000) return n.toLocaleString('en-US',{minimumFractionDigits:0,maximumFractionDigits:0});
  return parseFloat(n).toFixed(2);
}
function showErr(m) { const e=document.getElementById('errBox'); e.textContent='⚠ '+m; e.classList.add('on'); }
function hideErr() { document.getElementById('errBox').classList.remove('on'); }
function toast(m, type='') {
  const t=document.getElementById('toast');
  t.textContent=m; t.className='toast on'+(type?' '+type:'');
  clearTimeout(t._t);
  t._t=setTimeout(()=>t.classList.remove('on'),2500);
}
</script>
</body>
</html>
